
define('de.solygen/core/proto', [], function () {

    'use strict';

    if (typeof String.prototype.is !== 'function') {
        String.prototype.is = function (val) {
            var arr = (value.toString()).split(',');
            //TOOD: return fnd...
        };
    }

});
